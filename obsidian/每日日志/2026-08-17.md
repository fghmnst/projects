# 2026-08-17 每日日志

## 今日完成事项

### 主线 · 点灯收尾：PA0 LED 常态灭

1. **PA0 LED 改为常态灭**（commit：`0860b64`，12:36）：`DDM_test/Core/Src/main.c` 移除 500ms 闪烁循环（`HAL_GPIO_TogglePin` + `HAL_Delay`），改为初始化时置高电平 `GPIO_PIN_SET`——PA0 外接 LED 低电平点亮，高电平即常态灭。主循环清空为空转。
2. **ST-Link 掉线自愈 + 烧录闭环恢复**：8-16 遗留的 ST-Link attach 丢失问题在重启/插拔后自愈（lsusb 重新识别 `0483:3748`），本次改动编译烧录成功，板端验证 LED 常态灭。8-16 日志里「点运行不弹 stlink gdb 选项」的硬件前提已消除。

## 手动操作事项

1. **ST-Link 恢复**：重新插拔（或重启后）attach 恢复，验证 `lsusb | grep 0483` 见 `0483:3748`。*注意：本次为自愈，无确定性修复步骤，若复发需排查 usbipd bind/attach 状态。*
2. **编译 + 烧录**（沿用既有流程）：`cmake --build build/Debug` → `arm-none-eabi-objcopy -O binary build/Debug/DDM_test.elf build/Debug/DDM_test.bin` → 烧录（st-flash / 扩展按钮）。验证：LED 不亮（常态灭为预期行为）。

## 运行环境现状

### ✅ 已完成
- ST-Link 在线（attach 恢复，烧录通路可用）
- PA0 LED 常态灭代码已烧录（commit `0860b64`，工作树干净）

### ❌ 缺少/待办
- ST-Link 掉线**根因未定位**（自愈而非根治），usbipd 重启后失效仍是已知坑
- `DDM_test/.vscode/launch.json` 是否存在/是否需要，仍未确认（8-16 遗留）

## 计划/工作流待办

1. **主线下一步**：0003 PWM 控制舵机（下一课预告），或继续精读 `stm32f1xx_hal_conf.h`（0002b 末尾建议，8-16 遗留）。
2. **Doxyfile 去留**：仍在 `/tmp/opencode/doxygen/`，是否挪进 `DDM_test/` 并 git 跟踪待确认（8-16 遗留）。
3. **ST-Link 掉线监控**：若再次掉线，排查 usbipd bind 是否保留、WSL-dashboard attach 状态。

## 疑惑点

1. **ST-Link 掉线根因**：8-16 掉线、本次插拔/重启自愈，根因未明（usbipd Persisted 有 2 条 bind 记录，GUID 不同）——观察是否复发，复发时收集 usbipd 状态再定位。
2. **点灯收尾后主线形态**：点灯闭环已完成（GPIO 输出 + 延时/常灭），下一步进入 PWM——GPIO 与定时器知识衔接点。

## 关联

- [[30天学习 Index|30天学习 Index]]
- [[教学-STM32/reference/0002b-stm32f1xx_hal.h逐行精读与C语法复习|0002b 精读笔记]]
- [[教学-STM32/reference/术语表|STM32 术语表]]
- [[教学-STM32/lessons/0002-CubeMX工程结构解剖-DianDengMaster逐文件精读|0002 课]]
- [[TIL/TIL|TIL 索引]]
