# 2026-08-10

> 30 天学习计划 · 第 10 天（周一，深度日）
> 主题：STM32 Lesson 1 动手实践（CubeMX 点灯工程生成）+ Hermes 飞书迁移收尾 + AGENTS.md 重构 + S90_aim_ball 移出工作区

## 一、今日完成事项

### 主线（STM32）：Lesson 1 实践第一步 + 工具链验证反馈
- **工具链版本验证反馈**（昨天遗留待办）：`arm-none-eabi-gcc` / `cmake` 版本验证通过（09:44 commit `3af2477` 勾选清单）；VS Code 插件 4 个就位（`5d4553e`）
- **Lesson 1 动手 2 第一步：CubeMX 实践成功**——打开 CubeMX、首次新建 F103C8T6 工程（自动下载 STM32CubeF1 固件包）、按课配好 SYS(Serial Wire) / RCC(HSE 8MHz) / 72MHz 时钟 / PC13 GPIO_Output、**成功 Generate Code 生成点灯工程**（手动操作，详见「手动操作事项」）
- **详细代码编辑与烧录 → 明天进行**（时间关系）
- **st-flash 识别 ST-Link 失败**：晚间实测 `st-flash --version` 未列出 STLink，Lesson 0001 清单该项从 `[x]` 回退 `[ ]`（烧录链路未通，待明天处理）

### Hermes 云端：飞书迁移收尾（09:30-11:56 session）
- **补写昨日（08-09）缺失日志**
- **Obsidian 打不开排查**：非故障——FUSE 缺失导致直接启动挂载失败，标准命令 `APPIMAGE_EXTRACT_AND_RUN=1 ~/bin/Obsidian.AppImage >/dev/null 2>&1 &`；`obsidian` CLI 需 App 运行中才可用
- **平台迁移 QQ/微信 → 飞书落地**（时间线见 server agent.log）：07:01 最后一次 QQ 投递 → 10:30 `cron run` → 10:34/10:36 两次 `delivered to feishu` 验证通过；TIL 指南改名重写（`git mv` 保留历史，新增「平台选型」章节：QQ markdown 受限子集、微信 24h 主动消息限制）
- **AGENTS.md 系列重构**（一天 5 个 docs commit）：gitignore 加入又移除（恢复跟踪）→ 顶部新增「Agent 职责边界」章节 → 服务器设施描述与 `.hermes.md` 对齐（deepseek-v4-flash / 飞书白名单 / home channel / 检查点 / QQ 停用）→ **清除全部 tmux 内容** → **服务器操作改只读**（一切写操作命令化交付）
- **Obsidian 配置跨设备同步**（`be0214d`）：`.obsidian/` 改黑名单排除（仅 workspace.json / workspace-mobile.json / cache/），核心插件开关/主题/插件数据 12 文件入跟踪
- 今日 9 commits 全部推送 + server2 同步（服务器端已到 `be0214d`）

### 工程结构调整（晚间 session）
- **S90_aim_ball 参考仓库移出工作区** → `~/S90_aim_ball`（手动 mv；git clone 所得，remote 为原开源地址）
- **VS Code STM32 扩展「多个 CMake 项目」警告根因分析**：ST 扩展 `checkWorkspaceFolderConsistency` 要求工作区文件夹 = CMake 工程根（根下直接有 CMakeLists.txt）；`~/projects` 含 S90_aim_ball 的 2 个 CMakeLists（顶层 + `cmake/stm32cubemx/`）且父目录均不等于工作区 → 误报。移出后根治
- AGENTS.md 参考仓库描述更新（本次 commit）

## 二、手动操作事项（换电脑重搭环境必看）

| 序号 | 操作 | 原因/说明 |
|---|---|---|
| 1 | 工具链验证：`arm-none-eabi-gcc --version` / `cmake --version` / `st-flash --version` / `openocd --version` | 昨天重装 4 包后补反馈；gcc/cmake 通过，**st-flash 识别设备失败**（见疑惑 2） |
| 2 | CubeMX（Windows GUI）：打开 → New Project → 搜 F103C8T6（LQFP48）→ SYS 开 Serial Wire → RCC HSE=Crystal → Clock 72MHz → 芯片图点 PC13 设 GPIO_Output → Project Manager 填名（纯英文）/ Toolchain=CMake → Generate Code | Lesson 1 动手 2 步骤 1；首次建工程提示下载 STM32CubeF1 固件包，点确认 |
| 3 | `mv ~/projects/S90_aim_ball ~/S90_aim_ball` | 参考仓库移出工作区（避免 STM32 扩展 CMake 误报；不随 projects 仓库管理） |
| 4 | Obsidian 启动：`APPIMAGE_EXTRACT_AND_RUN=1 ~/bin/Obsidian.AppImage >/dev/null 2>&1 &`；先开 App 再跑 `obsidian` CLI | FUSE 坑；CLI 依赖 App 的 socket |
| 5 | 飞书迁移（用户侧，命令化交付执行）：改 `~/.hermes/.env`（FEISHU_APP_ID/SECRET/ALLOWED_USERS）+ `config.yaml`（feishu enabled + home channel，QQ/微信 enabled:false）→ `pkill -f "hermes_cli.main gateway"` → 验证 `hermes doctor` + 日志 `delivered to feishu` | 已全部完成并验证（10:34 起）；备份 `~/.hermes/.env.bak-[DATE_REDACTED]` |

## 三、运行环境现状

### ✅ 已完成
- 工具链 4 件套安装完成且版本输出正常（gcc 13.2.x / cmake / st-flash 1.8.0 / openocd 0.12.0）
- VS Code 插件 4 个就位（cpptools / cmake-tools / cortex-debug / serial-monitor）
- **CubeMX 可用，F103C8T6 点灯工程已生成**（生成位置待确认，见疑惑 1）
- Obsidian 启动链路正常（FUSE 坑已记）
- Hermes 飞书投递链路验证通过（10:34/10:36）；QQ 停用（最后一次 07:01 投递）
- 本地与 server2 仓库同 commit（`be0214d`）

### ❌ 缺少 / 待办
- **st-flash 识别 ST-Link**（usbipd 直通未配置，WSL 侧看不到设备）——烧录链路未打通
- 点灯工程 vscode 编译未做（明天）
- Lesson 0001 收尾：接线、烧录、蓝灯闪（动手 2/3）
- 微信/QQ 凭据残留清理确认（备份已留存，AGENTS.md 已注明不启用）

## 四、计划/工作流待办

- [ ] 明天 Lesson 1 继续：定位 CubeMX 工程 → vscode 打开编译（kit=arm-none-eabi，preset Debug）→ `build/` 下 .elf/.bin
- [ ] 明天 usbipd 直通：`usbipd bind`（ST-Link 2 interface 都要）→ WSL `usbip attach` → `lsusb` 见 0483:3748 → `st-flash --version` 列出 STLink；30 分钟搞不定 → Plan B Windows 侧 CubeProgrammer CLI
- [ ] 接线（SWDIO/SWCLK/GND/3.3V）→ 烧录 → 蓝灯闪；learning record 0003
- [ ] 下一课 0002：PWM 控制舵机（SG90 脉宽映射，TIM2 参考仓库 PA1 配置）
- [ ] 副线贪吃蛇：0001 课动手（hello.cpp）→ 0002 课（终端渲染与 vector）
- [ ] 飞书观察期：daily-digest 07:00 正常投递 2-3 天；QQ/微信配置清理确认
- [ ] 参考仓库后续使用方式：复刻时在 `~/S90_aim_ball` 外新建工程目录（不污染 clone）

## 五、我的疑惑（重点）

1. **CubeMX 点灯工程生成在哪**？Lesson 写路径选 `~/projects/`，但 /home/fgh 下找不到 led_blink/任何新 .ioc——CubeMX 是 Windows GUI，工程大概率生成在 Windows 侧。明天编译前需先定位（Windows 资源管理器找），并确定编译侧：vscode Remote-WSL 打开 Windows 路径工程？还是迁到 WSL 再开？
2. **st-flash 认不到 ST-Link 的根因**：未插设备？还是 WSL 无 usbipd 直通必然认不到？明天插上后先 `lsusb` 验证（预期 0483:3748），再走 usbipd 步骤。
3. **自建 CubeMX 工程的 CMake 配置**：参考仓库的 CMakePresets.json 有 `cmake/gcc-arm-none-eabi.cmake` toolchain 文件，CubeMX 自生成工程是否有同款 preset？vscode 打开后 CMake Tools 的 kit/preset 识别是否顺畅（Lesson 写「选 kit = GCC for arm-none-eabi」）？
4. **飞书长期可用性**：个人账号建应用有平台审核风险（TIL 已记），若以后被封，QQ/微信已停用，是否要留一个 Plan B 平台？

## 六、关联

- [[30天学习 Index]]
- [[每日日志/2026-08-09|昨日日志]]
- [[TIL/Hermes 云部署指南（飞书每日推送）]]
- [[教学-STM32/lessons/0001-工具链闭环与LED闪烁|STM32 Lesson 0001]]
- [[教学-STM32/MISSION.md|STM32 任务书]]
- [[术语表/术语表|术语表]]
