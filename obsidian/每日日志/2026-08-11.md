# 2026-08-11

> 30 天学习计划 · 第 11 天（周二，浅度日）
> 主题：DianDengMaster 工程接入工作区 + Obsidian 启动故障排障（T 态挂起根因）+ AGENTS.md 更新 + CMake 多项目警报新根因

## 一、今日完成事项

### DianDengMaster 工程接入（main commit `b6ae8ff`「想要成为点灯带师。」）
- 用户将 CubeMX 生成的点灯工程 **DianDengMaster/** 加入工作区并提交（**昨天疑惑 1 解决**：工程生成位置 = WSL 内 `~/projects/DianDengMaster/`）
- 结构确认（**重要坑**）：CubeMX `UnderRoot=false` + `ToolChainLocation=DianDengMaster` 导致**一个工程拆两个目录**：
  - 源码根 `DianDengMaster/`：`Core/`(Src/Inc)、`Drivers/`(HAL+CMSIS)、`DianDengMaster.ioc`、`.mxproject`
  - **CMake 工程根 = 嵌套 `DianDengMaster_1/`**：`CMakeLists.txt`、`CMakePresets.json`、`startup_stm32f103xb.s`、`STM32F103xx_FLASH.ld`、`cmake/stm32cubemx/CMakeLists.txt`（经 `../../../Core` 相对路径引用源码）
- 与昨天疑惑 3 呼应：CubeMX 自生成工程**自带** CMakePresets.json（vscode 打开后 CMake Tools 应可直接识别）

### Obsidian 启动故障排障（`obsidian-gui` 今天失效）
- **症状**：`obsidian-gui` 后只见 `[WARN:COPY MODE]`，GUI 不弹出
- **根因（两层）**：
  1. `[WARN:COPY MODE]` = AppImage runtime 良性提示（FUSE 不可用 → 解压运行；alias 强制 `APPIMAGE_EXTRACT_AND_RUN=1`），**与故障无关**
  2. 真凶：旧 alias `APPIMAGE_EXTRACT_AND_RUN=1 ~/bin/Obsidian.AppImage &` 后台运行但 **stdout/stderr 仍挂终端**，进程组被作业控制信号挂起（`ps` 见 **T 态**，`wchan=do_signal_stop`），GUI 在初始化前被冻结；且 `~/.config/obsidian/` 残留 **SingletonLock**（指向已无进程的 socket），不清掉会继续挡新实例
- **修复**：`kill` 掉 T 态实例 → 删 `~/.config/obsidian/Singleton{Lock,Cookie,Socket}` + `/tmp/scoped_dirIG0tcx` → **alias 加固**（`~/.bashrc:125`）：
  `nohup env APPIMAGE_EXTRACT_AND_RUN=1 ~/bin/Obsidian.AppImage >/tmp/obsidian.log 2>&1 &`
- **验证通过**：加固后启动进程为 S/Sl 态（正常），GUI 正常弹出

### AGENTS.md 更新（本次 commit）
1. 新增「练习项目：DianDengMaster」小节（结构坑、VS Code 单独开窗工作流、构建入口）
2. 「已知坑」更新：STM32 扩展多 CMake 误报的新元凶 = DianDengMaster 嵌套 CMake 根（S90 移出只解决一半）
3. Obsidian 小节：启动命令更新为加固版 + 注明 `[WARN:COPY MODE]` 良性

## 二、手动操作事项（换电脑重搭环境必看）

| 序号 | 操作 | 原因/说明 |
|---|---|---|
| 1 | Obsidian 故障清理（今天执行过，全部完成）：`kill <T态PID>` → `rm -f ~/.config/obsidian/Singleton{Lock,Cookie,Socket}` → `rm -rf /tmp/scoped_dirIG0tcx` → 重新启动 | 进程 T 态挂起 + 残留单实例锁；锁不删则新实例报「已在运行」 |
| 2 | `~/.bashrc` 第 125 行 alias 改加固版（已改）：`alias obsidian-gui='nohup env APPIMAGE_EXTRACT_AND_RUN=1 ~/bin/Obsidian.AppImage >/tmp/obsidian.log 2>&1 &'` | 必须 nohup + 重定向：`&` 后台不脱离终端会被作业控制挂起；日志查 `/tmp/obsidian.log` |
| 3 | VS Code 嵌入式开发**不要**在 `~/projects` 根窗口进行；一律 `code ~/projects/DianDengMaster` 打开该文件夹 | 嵌套 CMake 根触发 STM32 扩展「多个 CMake 项目」误报 |
| 4 | （待办）CubeMX 若重新生成 DianDengMaster：保持 `ToolChainLocation=DianDengMaster`，生成后目录结构不变 | 若想消除 `_1` 嵌套需改 CubeMX 配置，未验证，暂不折腾 |

## 三、运行环境现状

### ✅ 已完成
- DianDengMaster 工程入仓库（`b6ae8ff`），结构已摸清（源码根 + `_1` CMake 根）
- Obsidian 启动链路修复 + alias 加固，启动验证通过
- AGENTS.md 三处更新（DianDengMaster / 已知坑 / obsidian-gui）
- 昨天疑惑 1（工程生成位置）已解决

### ❌ 缺少 / 待办
- **st-flash 识别 ST-Link**（usbipd 直通未配置）——烧录链路仍未打通
- 点灯工程 vscode 编译未做（kit/preset 识别待实测，昨天疑惑 3 收尾）
- Lesson 0001 收尾：接线、烧录、蓝灯闪（动手 2/3）
- 飞书观察期（daily-digest 正常投递 2-3 天）未完成观察

## 四、计划/工作流待办

- [ ] DianDengMaster 用 `code ~/projects/DianDengMaster` 打开 → CMake Tools 选 kit（arm-none-eabi）+ preset（Debug）→ 编译出 .elf/.bin（验证昨天疑惑 3）
- [ ] usbipd 直通：`usbipd bind`（ST-Link 2 interface 都要）→ WSL `usbip attach` → `lsusb` 见 0483:3748 → `st-flash --version` 列出 STLink；30 分钟搞不定 → Plan B Windows 侧 CubeProgrammer CLI
- [ ] 接线（SWDIO/SWCLK/GND/3.3V）→ 烧录 → 蓝灯闪；learning record 0003
- [ ] 观察 obsidian-gui 加固版连续使用是否稳定（今天验证一次通过）
- [ ] 下一课 0002：PWM 控制舵机（SG90 脉宽映射，TIM2 参考仓库 PA1 配置）
- [ ] 副线贪吃蛇：0001 课动手（hello.cpp）→ 0002 课（终端渲染与 vector）

## 五、我的疑惑（重点）

1. **Obsidian T 态挂起的确切信号源**：已确认是作业控制类信号（do_signal_stop），但无法从现场断定是 SIGTSTP/SIGTTIN/SIGTTOU 哪个；加固版（nohup+重定向）从机制上根治，若再复发需抓 `/proc/<pid>/status` 与终端设置复盘
2. **DianDengMaster_1 嵌套能否消除**：CubeMX `UnderRoot=false` 的产物；想改成单一目录（CMakeLists 直接在 DianDengMaster/ 根）需动 CubeMX 配置，不确定哪个选项组合能达到，暂不折腾（现状可用）
3. **st-flash 认不到 ST-Link 的根因**：未插设备？还是 WSL 无 usbipd 直通必然认不到？插上后先 `lsusb` 验证（预期 0483:3748）再走 usbipd
4. **飞书长期可用性**：个人账号建应用有平台审核风险（TIL 已记），若以后被封，QQ/微信已停用，是否要留 Plan B 平台

## 六、关联

- [[30天学习 Index]]
- [[每日日志/2026-08-10|昨日日志]]
- [[TIL/Hermes 云部署指南（飞书每日推送）]]
- [[教学-STM32/lessons/0001-工具链闭环与LED闪烁|STM32 Lesson 0001]]
- [[教学-STM32/MISSION.md|STM32 任务书]]
- [[术语表/术语表|术语表]]
