# 2026-08-04

> 30 天学习计划 · 第 4 天
> 主题：GitHub 工作流打通 + Obsidian 环境搭建 + 工具盘点

## 一、今日完成事项

### GitHub 连接与仓库
- SSH 密钥：从 Windows（`/mnt/c/Users/[USERNAME_REDACTED]/.ssh/`）复制 `id_ed25519` 到 WSL `~/.ssh/` 并修正权限，认证成功（GitHub 用户名 `fghmnst`）
- git 全局身份设为 `fghmnst <[EMAIL_REDACTED]>`
- 创建**私有**仓库 `projects` 并推送：
  - `f4cc3c1` init（AGENTS.md + 规划文档 + .gitignore）
  - `517f629` AGENTS.md 补知识库位置

### Obsidian 环境（WSL）
- 安装 Linux 版 Obsidian（AppImage，`~/bin/Obsidian.AppImage`，WSLg 跑 GUI）
- 启用官方 CLI（`~/.local/bin/obsidian`，需 App 运行中）
- 创建 vault `/home/fgh/obsidian/`：入口 `30天学习 Index.md` + `每日日志/`、`模块笔记/`、`TIL/`、`术语表/`
- 更新 `obsidian-vault` 技能路径 → 新 vault
- 补 TIL 首条：《Obsidian AppImage 依赖缺失》

### 主项目
- 克隆参考仓库 `projects/S90_aim_ball/`（分支 master）
- 读完 `pid.py`（PD 控制，37 行）、`ball_track.py`（OpenCV 追踪+串口，108 行）、`main.c`（UART+sscanf+PWM 核心，356 行）——教学素材就绪

### 工具盘点（工作流方向）
- 调研 vscode / obsidian / opencode 的能力、插件、指令、skills、学习资料（详见对话记录）

### 环境探测
- 查清运行环境缺什么（见第三节）

## 二、手动操作事项（换电脑重搭环境必看）

> 按顺序执行；sudo 需要密码，我的终端无法交互输入，由用户手动执行。

| 序号 | 操作 | 原因/说明 |
|---|---|---|
| 1 | `sudo apt install -y libnspr4 libnss3 libasound2t64` | Obsidian AppImage（Electron）运行库，WSL 最小化缺失 |
| 2 | `sudo apt install -y python3.12-venv` | Ubuntu 创建 venv 必需（ensurepip） |
| 3 | **待执行** `sudo apt install -y cmake gcc-arm-none-eabi` | STM32 编译链，尚未装 |
| 4 | **待执行**（可选）`sudo apt install -y openocd` | WSL 内配合 ST-Link 烧录/调试 |
| 5 | Obsidian GUI：Settings → General → 启用「Command line interface」 | CLI 注册到 `~/.local/bin/obsidian` |
| 6 | `gh auth login`（浏览器授权，git 走 SSH） | gh 建仓需要，Token scopes 含 repo |
| 7 | 采购 **ST-Link V2**（约 10 元） | 烧录必需，已决定购买，等货 |
| 8 | SSH 密钥复制：`cp /mnt/c/Users/[USERNAME_REDACTED]/.ssh/id_ed25519 ~/.ssh/` + `chmod 600` | WSL 拒绝 0777 权限的私钥 |
| 9 | git 身份：`git config --global user.name fghmnst` / `user.email [EMAIL_REDACTED]` | commit 署名 |

启动 Obsidian 的完整命令：`APPIMAGE_EXTRACT_AND_RUN=1 ~/bin/Obsidian.AppImage`（24.04 无 libfuse2）

## 三、运行环境现状

### ✅ 已完成
- git / SSH / GitHub 私有仓库（`git@github.com:fghmnst/projects.git`）
- Obsidian App + CLI + vault（`/home/fgh/obsidian`）
- 参考仓库克隆（`projects/S90_aim_ball`）
- `python3.12-venv`（系统包，已装）
- vscode 扩展：`cpptools`、`cmake-tools`、`cpptools-extension-pack`

### ❌ 缺少 / 待办
- `.venv` 残缺（之前失败残留，无 pip），**需重建** + 装 `opencv-python pyserial numpy`
- `cmake`、`gcc-arm-none-eabi`（→ 手动操作第 3 条）
- `openocd`（可选）
- vscode 扩展：`cortex-debug`（依赖问题装失败）、`serial-monitor` 未装
- ST-Link V2（待采购、待货）
- USB 通路方案未定（usbipd-win 透传 vs Windows 侧烧录）
- STM32CubeMX（参考仓库自带 `.ioc`，暂缓）

## 四、工作流工具盘点报告 · 待办

- [ ] 盘点报告是否存进 obsidian / projects 仓库？（现在正在写日志，报告本体未存）
- [ ] 想先深入哪个工具？（opencode 自定义命令 / vscode Tasks 自动化 / obsidian 模板）
- [ ] 教学模式约定"默认开启"定稿并写进 AGENTS.md
- [ ] 参考仓库的嵌套 `.git` 如何处理（纳入单仓 vs 保留独立）
- [ ] 采购 ST-Link 后：烧录 LED 闪烁 → 全链路验证 → W1 里程碑（串口转舵机）

## 五、我的疑惑（重点）

1. **环境的作用与必要性**：目前只按命令装了环境，但对「为什么需要它们」还不清楚——
   - `cmake` / `gcc-arm-none-eabi` 在构建链中各自干什么？
   - OpenCV / pyserial / venv 分别解决什么问题？
   - STM32CubeMX 生成 `.ioc` 与 vscode 手写代码的关系？
2. **WSL 与 USB 设备**：WSL2 访问 CH340 / ST-Link 的可行路径有哪些，各自的坑？
3. **教学模式**：说「教学」时 opencode 应该怎么做才算有效（讲→做→考→沉淀），细节待定稿。
4. **仓库边界**：知识库在 obsidian、代码在 projects——两者哪些内容该进 git、哪些不该？

## 六、关联

- [[30天学习 Index]]
- [[TIL/Obsidian AppImage 依赖缺失]]
- [[术语表]]
