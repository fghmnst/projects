# 2026-08-19（第 19 天）

> 深度日。主线：双舵机接线 → 串口 + PWM 固件 → WASD 键盘测试打通。

## 今日完成事项

1. **接线理念确认**：最小系统板横跨面包板 + 3.3V/GND 电源轨作用（为外设铺电源总线），笔记已存 `模块笔记/面包板接线.md`
2. **双舵机接线方案定稿**：18650 直连双舵机（临时方案）+ 共地原理 + 双电源正极注意事项，笔记 `模块笔记/舵机接线.md`
3. **DDM_test 工程改造**（CubeMX + 手写用户代码）：
   - HSI 8MHz → **HSE 8MHz ×9 = 72MHz**
   - TIM2 CH1(PA0) + 新增 **CH2(PA1)**：Prescaler 71 / Period 19999 / Pulse 1500 → 50Hz 舵机 PWM
   - 新增 **USART1**（PA9/PA10，115200，轮询接收）
   - main.c 用户代码：删呼吸灯 → PWM Start ×2 + 归位 → `sscanf` 解析 `X增量,Y增量\n` → 限位 500–2500（commit `f982ef5`）
4. **烧录 + 串口链路验证**：minicom 手动发数 → 舵机响应；WASD 测试 `fire_control/servo_test.py`（termios 单键，FLIP 方向翻转）——**X 方向 OK，Y 方向反了 → `FLIP_Y=True` 修复，四个方向全部正确**
5. **projects 目录整理**（commit `98a0c3f`，已推送）：删 DianDengMaster 旧目录（git 历史可找回）、`servo_test.py` → `fire_control/`、CppSnake 骨架纳入 git、30天对话记录 → `obsidian/归档/`、清理 `__pycache__/`、AGENTS.md 全面更新（DDM_test 平铺结构/外设现状/fire_control/参考仓库只读/已知坑消除）
6. **参考仓库 `~/S90_aim_ball` 设为只读**（`chmod -R a-w`），servo_test.py 复刻至本仓库

## 手动操作事项

- `sudo usermod -aG dialout $USER`（串口永久权限，**待重新登录 WSL 生效**）
- `sudo chmod 666 /dev/ttyUSB0` ×2（临时绕过，设备重枚举后失效——已记录 TIL）
- CubeMX GUI 操作：HSE/时钟树/TIM2 CH2/USART1 配置 + Generate Code（ToolChainLocation=DDM_test 不变）
- ST-Link 烧录 `build/Debug/DDM_test.bin`（VS Code F5）
- 硬件接线：舵机×2 + 18650 + CH340（PA9/PA10 交叉接线、GND 轨共地）

## 运行环境现状

**✅ 已完成**
- DDM_test：72MHz + 双舵机 PWM（PA0/PA1）+ USART1 串口解析，编译 0 warning，烧录成功
- 双舵机 WASD 测试四个方向全部正确（`fire_control/servo_test.py`）
- 串口链路（PC → CH340 → USART1 → TIM2 PWM）全通
- projects 目录结构已整理、AGENTS.md 已更新、参考仓库已只读

**❌ 缺少/待办**
- **舵机供电升级**：单节 18650 直连电压不足（3.7–4.2V < 4.8V 下限），Y 舵机力矩不足——待 5V/2A 独立电源或升压模块到货
- dialout 组未生效（未重登 WSL），当前靠 chmod 临时权限
- 云台机械结构（双舵机固定架）未组装

## 计划/工作流待办

- [ ] 重登 WSL 让 dialout 永久生效
- [ ] 电源零件到货后升级供电：18650 → 升压模块 5V（或 2×18650 + 降压），重测 Y 舵机力矩
- [ ] 复刻 `ball_track.py`/`pid.py` 到 `fire_control/`（参考仓库只读后，视觉追踪代码从参考仓库复刻）
- [ ] 组装双舵机云台机械结构
- [ ] 测试完成后确认：PWM 步长 50 是否适合追踪场景（PID 输出映射）

## 疑惑点

- SG90 在 3.7V 下「完全不动 vs 能动能转但力矩弱」的边界在哪？Y 舵机明显比 X 舵机迟钝，除电压外是否与舵机个体差异有关？（待供电升级后复测对比）
- CubeMX 重新生成后工程从嵌套变平铺——是改名+重生成的连带结果，还是 CubeMX 版本/设置变化？当前平铺结构是否符合 CubeMX 后续再生成（不产生嵌套）？（下次生成时验证）

## 关联

- [[模块笔记/舵机接线|舵机接线]] · [[模块笔记/面包板接线|面包板接线]] · [[TIL/WSL 串口权限 dialout 组与 chmod 临时方案|WSL 串口权限]] · [[30天学习 Index|30天学习 Index]]
