# 2026-08-18 每日日志

## 今日完成事项

### 主线 · 呼吸灯：CubeMX 工作流恢复 + TIM2 PWM（PA0，1kHz，HSI 8MHz）

1. **频率答疑（8MHz→72MHz）**：明确是系统主频 SYSCLK，HSE 8MHz × PLL×9 = 72MHz；决定**保持 8MHz**（72MHz 需先确认板上晶振，留待后续任务）。
2. **手改 vs CubeMX 决策（2026-08-14 决策逆转，暂定可逆）**：发现 `.ioc` 已与代码严重脱节（.ioc 冻结于 PA6 LED，实际手改为 PA0/PC13），恢复 CubeMX 工作流，USER CODE 段纪律复活。
3. **Linux 版 CubeMX 6.18.1 安装闭环**：
   - ST 官网下载（Windows Downloads，`SetupSTM32CubeMX-6.18.1-Lin-x86_64.zip`，548MB ELF GUI 安装器）
   - unzip 缺失 → 解压工具对比（unzip/7zip/bsdtar/unar）→ 选 unzip（教程通用）+ 7zip 兜底可选
   - 安装至 `~/STM32CubeMX`（免 sudo）；版本与 `.ioc` 完全一致，无迁移提示
4. **\\wsl$ 排障（未闭环）**：`net use Z: \\wsl.localhost\...` 错误 64 两次——第一次发行版名错（应为 `Ubuntu-24.04`，`wsl -l` 可查）；第二次定位根因：**wsl.localhost 未注册 Windows DNS**（wslservice Running、445 监听、ping/nslookup 均失败）。建议 `wsl --shutdown` 未执行——最终走 Linux CubeMX 路线（WSLg 本地路径）绕开，SMB 问题不再阻塞。
5. **CubeMX GUI 配置 TIM2**（全程手动）：清 PA6 旧残留 → PA0 选 `TIM2_CH1` → Timers/TIM2：Internal Clock、PSC=7、ARR=999、PWM Generation CH1（Mode PWM1、Pulse=0、极性 High）→ 时钟树不动 → Generate Code 成功。生成产物检查：单层目录（嵌套坑未复现）、`hal_tim.c/hal_tim_ex.c` 自动挂 CMake、PA0 复用（AF_PP）在 `HAL_TIM_MspPostInit`。
6. **答疑：ARR/Pulse 是计数值非百分比**；TIM2 是 **32 位定时器**（F103 的 TIM2/TIM5 为 32 位）；CubeMX 参数框支持四则表达式（如 `8-1`）。占空比 = Pulse ÷ (ARR+1)。
7. **呼吸循环写入 `main.c` USER CODE 段**（严格纪律）：`HAL_TIM_PWM_Start` + CCR 三角波渐变（±1/2ms，单程 2s，完整呼吸 4s）。
8. **编译通过**：FLASH 7960B（12.15%）、RAM 1656B（8.09%）；bin 已生成 `DDM_test/build/Debug/DDM_test.bin`。
9. **git commit `c8a20e0`**（12 files，+16758）已推送 origin main；`main.h` 手改的 PC13 宏被生成重写移除（预期行为）。
10. **统一下载目录约定**：`~/Downloads/`（zip 归档），安装产物解压至 `~/`。

## 手动操作事项

1. **ST 官网下载 CubeMX Linux 版**：浏览器 + ST 账号登录；「点击下载没反应」排查链：弹窗拦截 → 广告插件 → 登录态 → 代理分流（未定位到具体原因，换环境/重试后成功）。
2. **安装 unzip**：`sudo apt install unzip`（WSL 精简装不含；验证 `unzip -v | head -1`）。
3. **解压安装包**：`unzip ~/Downloads/SetupSTM32CubeMX-6.18.1-Lin-x86_64.zip` → 运行 ELF 安装器（GUI 向导）→ 装至 `~/STM32CubeMX` → 启动 `nohup ~/STM32CubeMX/STM32CubeMX >/tmp/cubemx.log 2>&1 &`。
4. **CubeMX GUI 配置 + Generate Code**（参数见「今日完成」第 5 条，全程 GUI）。
5. **net use 映射尝试**（失败）：`net use Z: \\wsl.localhost\Ubuntu-24.04\home\fgh\projects\DDM_test` → 错误 64；根因 wsl.localhost DNS 未注册，未修复即绕开。
6. **ST-Link 烧录验证呼吸灯（待办，未做）**：attach 后 `STM32_Programmer_CLI -c port=SWD mode=UR -w build/Debug/DDM_test.bin` → 目视 PA0 LED 呼吸。

## 运行环境现状

### ✅ 已完成
- CubeMX 6.18.1 Linux 版已装（`~/STM32CubeMX`，WSLg GUI 正常，版本与 .ioc 一致）
- unzip 可用；下载统一进 `~/Downloads/`
- TIM2 PWM 初始化 + 呼吸循环编译通过（commit `c8a20e0` 已推送）
- 工具链齐备：CMake preset（build/Debug）+ `STM32_Programmer_CLI`（烧录用）

### ❌ 缺少/待办
- 呼吸灯**烧录 + 目视验证**未完成（PA0 LED 极性待确认）
- `wsl.localhost` DNS 注册未修复（`net use Z:` 未成功；Linux 路线暂时绕开）
- 72MHz 时钟配置未做（决定保持 8MHz；前置：板上晶振确认）

## 计划/工作流待办

1. **烧录验证呼吸灯**（ST-Link attach → `STM32_Programmer_CLI` 烧 bin → 目视；若极性反，翻转渐变方向一行）。
2. **72MHz 时钟配置**（下一任务候选：HSE/PLL 或 HSI 方案，先确认板上 8MHz 晶振）。
3. **dlsync alias**（`cp -u /mnt/c/Users/[USERNAME_REDACTED]/Downloads/* ~/Downloads/`，统一下载目录机制）未确认启用。
4. **wsl.localhost 修复**（`wsl --shutdown` 后重试 `net view \\wsl.localhost`）或明确放弃 \\wsl$ 路线。
5. **USER CODE 纪律**：CubeMX 工作流已恢复，后续所有手改必须落在 USER CODE 段（本次已验证生成覆盖 main.h 的行为）。
6. **主线后续**：USART 串口（火控云台）→ PID；副线贪吃蛇。
7. 8-17 遗留：Doxyfile 去留、`launch.json` 是否必要（若走 STM32_Programmer_CLI 烧录可不要）。

## 疑惑点

1. **wsl.localhost 未注册根因**：WSL 2.7.10 + Win 11 24H2，wslservice Running、445 监听，但 DNS 无该记录——WSL hostname 注册机制失效，`wsl --shutdown` 是否修复未验证。
2. **PA0 外接 LED 是否真接、点亮极性未知**——烧录后见分晓（现假设高电平点亮，Pulse 大=亮）。
3. **板上是否有 8MHz 外部晶振**——72MHz 方案的前置条件，需目视确认。
4. **CubeMX 下载「点击没反应」**：未定位具体原因（弹窗/插件/登录/代理均可能），本次靠重试成功，未形成确定性结论。

## 关联

- [[30天学习 Index|30天学习 Index]]
- [[教学-STM32/lessons/0002-CubeMX工程结构解剖-DianDengMaster逐文件精读|0002 课]]
- [[教学-STM32/reference/术语表|STM32 术语表]]
- [[TIL/TIL|TIL 索引]]
- [[术语表/术语表|术语表]]
