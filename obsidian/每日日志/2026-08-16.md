# 2026-08-16 每日日志

## 今日完成事项

### 主线 · 头文件精读 + Doxygen 文档（纯软件日，无硬件）

1. **精读 `stm32f1xx_hal.h`**（357 行）：讲解文件结构（HAL 图书馆中央大厅比喻：include guard / extern "C" / uwTick 时基 / DBGMCU 调试宏 / 函数声明），并讲解了 C 语法（宏函数、typedef enum、volatile、extern、include guard、Doxygen 注释等）。
2. **产出 0002b 精读笔记**：`教学-STM32/reference/0002b-stm32f1xx_hal.h逐行精读与C语法复习.md`——全文源码 + C 语法批注 + 注释中文翻译 + 结构总览 + 用好指南 + 5 个练习。同时更新了术语表（新增「头文件精读与 C 语法」分组 15 条）和 0002 课「关联」区。commit：`faf61c3`。
3. **Doxygen 文档生成闭环**：
   - 用户安装 doxygen 1.9.8 + graphviz 2.43.0（apt）
   - 配置 `/tmp/opencode/doxygen/Doxyfile`（INPUT = HAL Inc/Src + CMSIS，EXTRACT_ALL，SOURCE_BROWSER，调用图）
   - 生成 `DDM_test/build/doxygen/html/`（69MB、2006 文件，含 stm32f1xx_hal.h 文档页与调用图）
4. **解决了 PowerShell 打开文档报错**：`wslpath` 是 WSL 命令，PowerShell 里要用 UNC 路径 `explorer.exe "\\wsl.localhost\Ubuntu-24.04\home\fgh\projects\DDM_test\build\doxygen\html\index.html"`。
5. **讲解 main.c 结构 + C 语法**（include → main → 初始化 → 死循环骨架），并解释 Doxygen 注释与普通注释的区别、HAL 与 SPL 的区别及历史原因（SPL 每系列一套库，HAL 一套 API 通吃 + CubeMX 生态）。
6. **江科大点灯代码报错诊断与修复**：用户抄的 SPL 代码（`GPIO_Init`/`RCC_APB2PeriphClockCmd`/`GPIO_Mode_Out_PP`）与 HAL 工程不兼容——给出逐行 SPL→HAL 对照表并改写，用户手动修改后**编译烧录成功，PA0 LED 500ms 闪烁**。
7. **排查"点运行不再弹 stlink gdb 选择"**（未解决，见疑惑点）：调查 VS Code workspace 结构（Windows 侧 VS Code + Remote-WSL）、STM32 CubeIDE 扩展集（含 stm32cube-ide-debug-stlink-gdbserver）、usbipd 状态。**结论：ST-Link 当前不在线**（usbipd Connected 无设备、WSL lsusb 无 0483、Persisted 有 bind 记录 = attach 丢失，AGENTS 已知坑）。

## 手动操作事项

1. **安装 doxygen + graphviz**（sudo apt）：`sudo apt install -y doxygen graphviz`；验证 `doxygen --version`（1.9.8）、`dot -V`（2.43.0）。坑：官方源慢，可换清华镜像（本次未换，下载慢但完成）。
2. **PowerShell 打开文档**：`explorer.exe "\\wsl.localhost\Ubuntu-24.04\home\fgh\projects\DDM_test\build\doxygen\html\index.html"`（不要用 wslpath）。
3. **修改 main.c**（SPL→HAL 翻译）：`RCC_APB2PeriphClockCmd`→`__HAL_RCC_GPIOA_CLK_ENABLE()`；`.GPIO_Pin`→`.Pin`、`GPIO_Pin_0`→`GPIO_PIN_0`、`.GPIO_Mode`→`.Mode`、`GPIO_Mode_Out_PP`→`GPIO_MODE_OUTPUT_PP`、`.GPIO_Speed`→`.Speed`、`GPIO_Speed_50MHz`→`GPIO_SPEED_FREQ_LOW`；`GPIO_Init`→`HAL_GPIO_Init`、`GPIO_ResetBits`→`HAL_GPIO_WritePin`；补回 `HAL_Init()` 和 `= {0}` 清零。
4. **编译 + 烧录成功**：`cmake --build build/Debug` → objcopy 转 bin → st-flash 烧录 → PA0 LED 500ms 闪烁。
5. **明天待办（手动）**：插好 ST-Link → WSL-dashboard Attach → `lsusb` 验证 `0483:3748` → 回 VS Code 点运行验证弹调试器选择。

## 运行环境现状

### ✅ 已完成
- doxygen 1.9.8 + graphviz 2.43.0（文档生成工具链闭环，Doxyfile 在 `/tmp/opencode/doxygen/Doxyfile`，重跑 `doxygen /tmp/opencode/doxygen/Doxyfile` 即可刷新）
- 编译烧录闭环：PA0 LED 闪烁（SPL→HAL 翻译后）
- Doxygen 文档：`DDM_test/build/doxygen/html/index.html`
- 0002b 精读笔记 + 术语表 15 条新术语（已 commit `faf61c3`）

### ❌ 缺少/待办
- ST-Link 不在线（usbipd attach 丢失），STM32 扩展"运行"无法弹调试器选择——明天修复
- `DDM_test/.vscode/launch.json` 不存在（若用户点的是 F5 调试，可能需要；若用 STM32 扩展按钮则不需要）

## 计划/工作流待办

1. **ST-Link attach 排查**（明天首项）：插硬件 → usbipd attach（WSL-dashboard）→ lsusb 验证 → 点运行验证。
2. **确认用户"点击运行"是哪个入口**：STM32 扩展运行按钮 vs F5/运行和调试——决定是否要建 launch.json。
3. **Doxyfile 去留**：现在在 `/tmp/opencode/`（临时区），若想长期保留可挪进 `DDM_test/` 并 git 跟踪（待用户确认）。
4. 主线下一步：0003 PWM 控制舵机（下一课预告），或继续精读 `stm32f1xx_hal_conf.h`（0002b 末尾建议）。

## 疑惑点

1. **为什么点运行不再弹 stlink gdb 选项？**（已诊断未验证）——ST-Link 不在线导致扩展探测不到调试器。明天 attach 后验证；若不弹再查扩展输出面板和 launch.json。
2. **ST-Link 是什么时候掉的？** 今天烧录成功时还在线，之后可能拔插/重启导致 attach 丢失。usbipd Persisted 有 2 条 bind 记录（GUID 不同），原因待查。
3. **江科大教程与 HAL 的翻译规律**：已建立对照表（SPL→HAL 字典），后续看江科大教程代码可复用此表。

## 关联

- [[30天学习 Index|30天学习 Index]]
- [[教学-STM32/reference/0002b-stm32f1xx_hal.h逐行精读与C语法复习|0002b 精读笔记]]
- [[教学-STM32/reference/术语表|STM32 术语表]]
- [[教学-STM32/lessons/0002-CubeMX工程结构解剖-DianDengMaster逐文件精读|0002 课]]
- [[TIL/TIL|TIL 索引]]
