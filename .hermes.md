# 云端 Hermes 项目指引

> 本文件是**云端服务器**（RainYun，服务器信息见 `~/.ssh/config`，不写本文件）上 Hermes Agent 的工作区指引。
> 仅 Hermes 读取（`.hermes.md` 命名，opencode 等工具不会加载）。
> 本机 WSL 的 opencode 指令见仓库内 AGENTS.md。
> 加载隔离依据：Hermes 上下文文件发现顺序 `.hermes.md` → `AGENTS.md` → `CLAUDE.md`，先匹配先生效——云端 Hermes 只加载本文件，AGENTS.md 不会污染云端。

## 0. 公开仓库安全（2026-08-20 起强制，Hermes 也须遵守）

- **`~/projects`（GitHub `projects`）与用户另一个仓库 `Novice-Village-Elite-Monsters` 均为公开仓库**，commit 立即对外可见，**严禁写入隐私信息**：
  - 凭据/密钥/Token（`.env` 内容、API key、App Secret 等）
  - 服务器 IP / SSH 端口 / 云账号信息（含本文件原载的 IP/用户名组合）
  - 个人标识：邮箱、OpenID、频道/群 ID、白名单值、Windows 用户名、手机号；成绩、排名、校名等个人信息
- 写文档一律「写位置不写值」：IP 写「见 `~/.ssh/config`」、凭据写「`~/.hermes/.env`」、白名单/群 ID 写「`~/.hermes/config.yaml` 的 `platforms.feishu`」。
- 本文件 `.hermes.md` 已按安全规范清洗并纳入 git 跟踪（2026-09-10）；修改时仍须遵守上述规则，`git add` 前自查。

## 1. 项目定位（2026 秋起）

本工作区职责：**辅助用户规划大二学习路径，并记录相关项目进度**。当前阶段：**大二上（2026 秋）**。

- **唯一事实源**：`obsidian/2026-2027 大二上学期学习计划.md`（目标 / 时间预算 / 双轨策略 / 阶段门 / 风险）。计划变更以该文档 + 周复盘为准，不口头漂移。
- 本学期主线：C 阶梯（P1→P3，代码 `C-tools/`）、云台 2.0（`gimbal2/`，W11 点火）、CET-6（12 月）、五门校内课双轨；C++ 寒假启动。
- 阶段门：W4 轻门 / W9-10 期中门（四决策点）/ W16 云台 2.0 验收 / W18 期末复盘。
- 历史：暑假 30 天计划（教学-STM32 云台复刻、教学-贪吃蛇）已归档；DDM_test 冻结（2026-08-23）。

## 2. 知识库与 Obsidian

- 知识库（obsidian vault）位于 **`/home/fghmnst/projects/obsidian`**（随本仓库 git 管理，`.obsidian/` 配置跨设备同步、仅设备相关文件与二进制被 .gitignore 排除）。
- 结构：`2026秋 Index.md`（学期入口）、`2026-2027 大二上学期学习计划.md`、`每日日志/`、`周复盘/`、`课程笔记/`、`TIL/`、`术语表/`、`模块笔记/`、`教学-STM32/`、`教学-贪吃蛇/`、`阶段回顾/`、`归档/`、`somezhishi/`（子库，另有自己的 AGENTS.md 规则）。
- **目录约定**：每个目录有同名索引笔记；Wikilink 一律写**显式路径**（如 `[[术语表/术语表|术语表]]`），不用短名——vault 存在多个同名笔记，短名会歧义。
- **云端访问方式**：云端无 Obsidian App，直接读写 markdown 源文件（git 跟踪保证一致性）。涉及 wikilink 显式路径约定时必须遵守上述规则。

## 3. 云端运行环境（Hermes 自身）

- **Hermes**：provider `deepseek`，模型 `deepseek-v4-flash`；密钥在 `~/.hermes/.env`，非密钥配置在 `~/.hermes/config.yaml`。
- **Gateway**：systemd 系统服务 `hermes-gateway`（开机自启）。改配置后重启：`sudo systemctl restart hermes-gateway`（sudo 需密码，交用户手动执行）。
- **飞书机器人**（当前唯一消息平台，2026-08-10 起）：
  - 平台 `feishu`，WebSocket 长连接模式（无需公网入口）
  - 白名单：`FEISHU_ALLOWED_USERS`（值在 `~/.hermes/.env`，不写本文件）
  - 投递目标（home channel）：`config.yaml` 的 `platforms.feishu`（值不写本文件）
  - 飞书消息按 post 富文本渲染，markdown 自动降级纯文本（渲染失败不会乱码）
- **QQ / 微信**：**已停用**（2026-08-10 迁移至飞书）。config.yaml `enabled: false` + .env 凭据已注释，备份在 `~/.hermes/.env.bak-*`。不要重新启用，除非用户明确要求。
- **cron 任务 `daily-digest`**（`0 7 * * *`，deliver=feishu，workdir=`/home/fghmnst/projects`）：git pull → 读昨日日志 → 生成「昨日小结+今日待办」→ 推飞书。
- **文件系统检查点**：已启用（`checkpoints.enabled: true`），`/rollback` 可恢复被改坏的文件。
- **日志**：`~/.hermes/logs/gateway.log`（连接/消息）、`agent.log`（cron 执行/投递）。网关状态：`systemctl status hermes-gateway`。

## 4. 每日联动

- 用户每晚 commit 每日日志 → 次日 7:00 cron 推送依赖 `git pull` 拉到最新日志（不提交就读不到）。
- 修改 vault 内容后应顺手 `git commit`（作为安全网）。

## 5. 已定的技术决策（不要推翻）

- STM32 工具链：**vscode + STM32CubeMX(生成代码) + CMake**。
- 语言线：**C 是主线语言**（STM32 HAL + C 阶梯）；**C++ 寒假启动**（触发：C 阶梯达标）；Python 定位工具语言（视觉/脚本），本学期随课。
- 烧录器：用户已有 ST-Link V2（SWD 烧录）。烧录通路：usbipd-win 直通 WSL（Plan B：Windows 侧 CubeProgrammer CLI，永不阻塞）。
- 视觉方案：不沿用被否定的 HSV 快照方案；W11 启动周做选型门（见 `obsidian/TIL/视觉目标追踪方案对比.md`）。

## 6. 已知坑（直接相关）

- SG90 舵机虚位大、有死区：需 PD 控制 + 软件死区（误差 <40px 停止调整）。
- 激光头与摄像头物理不重合导致打偏：需要 `OFFSET_X`/`OFFSET_Y` 视差补偿。
- 2×SG90 需 5V/2A 独立供电，不要全从板子 USB 口取电。

## 7. 每日日志规范（2026-09-10 起三栏制）

- 每天一篇，写在 `obsidian/每日日志/YYYY-MM-DD.md`，当天结束时更新。
- **三栏**：
  1. **今日推进**：做了什么（含关键 commit/命令）。
  2. **卡点**：卡住的问题；需要用户手动做的事（sudo/浏览器/GUI/采购等）详录到命令级。
  3. **明日一击**：明天最重要的一件事。
- 底部加 `[[wikilinks]]` 关联（Index / TIL / 术语表）。
- 当天写完随代码一起 `git commit`。

## 8. 工程惯例

- 无 CI/lint/测试配置，验证方式 = vscode 编译 + 烧录 + 串口观察（PC 端脚本 = 实际运行）。
- 每天 `git commit` 作为安全网。
- **执行权限（云端约定）**：Hermes 可直接执行常规操作（文件/终端/配置/git）；**sudo 类操作例外**——服务器 sudo 需密码，一律输出可复制的命令行交用户手动执行，不代为执行。
- **安装**：非 sudo 安装可直接执行（如 venv 内 pip）；系统级安装（apt 等）走 sudo 例外规则，给出命令行 + ①验证手段 ②可能的问题与解法（含 Plan B）。
- 学习节奏：**20h/周**，主战场周五+周末；志愿者周砍序见计划文档；健身 6h/周（听力叠加）。
- 报告语言：中文。
